[package]
name = "bwa-mem3-rs"
description = "Safe Rust API for bwa-mem3 alignment with caller-owned parallelism"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
categories = ["science", "api-bindings"]
keywords = ["bioinformatics", "bwa", "alignment", "bam", "ngs"]
readme = "README.md"

[lib]
path = "src/lib.rs"

[features]
default = []
# Passthrough to bwa-mem3-sys/debug-poison; see that crate's feature docs.
debug-poison = ["bwa-mem3-sys/debug-poison"]

[dependencies]
bwa-mem3-sys = { version = "0.2.3", path = "../bwa-mem3-sys" }
thiserror = "1"
libc = "0.2"

[dev-dependencies]
rayon = "1"
rstest = "0.18"
tempfile = "3"

[lints]
workspace = true
